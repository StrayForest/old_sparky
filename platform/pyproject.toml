[tool.ruff]
target-version = "py312"

[tool.ruff.lint.per-file-ignores]
# Standalone operator tools deliberately prepend platform/ to sys.path before
# importing repository packages so they also work from immutable releases.
"tools/*.py" = ["E402"]
