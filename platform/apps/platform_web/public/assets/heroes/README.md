Hero icon assets live here.

The web UI resolves icons by English hero name, normalized to lowercase kebab
case, for example `Grey Talon` -> `grey-talon.svg` and `Mo & Krill` ->
`mo-and-krill.svg`. Missing files fall back to `placeholder.svg`.
